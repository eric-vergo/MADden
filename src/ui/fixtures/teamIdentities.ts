// The league identities the UI fixtures render. src/data/teams.ts is the
// authoritative list (pure data, owned by the content workstream); this module
// is the single seam so a screen or fixture never reaches across on its own.

export { TEAM_IDENTITIES } from '../../data/teams';
